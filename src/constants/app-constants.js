/**
 * @fileOverview defines valid app actions to be dispatched and handled.
 */
let keyMirror = require('keymirror');

export default keyMirror({
  APP_CLIENT_SETUP: null,
  APP_LOGIN: null,
  PLAY_SUCCESS_ANIM: null,
  PLAY_FAIL_ANIM: null
});
